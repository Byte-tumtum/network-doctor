# Go Binary
/network-doctor
/netdoc
/netdoc-sim

# Saved reports — recording demo.tape presses 'w', which drops one here
/network-doctor-*.txt

# GoReleaser
dist/
# Vendored Go deps + the tarball of them COPR builds against, both produced by
# release-time hooks. Neither belongs in the source tarball.
/vendor/
/network-doctor-*-vendor.tar.gz

# Graphify
graphify-out/

# Node (nothing here is Go's business)
node_modules/
package.json
package-lock.json

# AI Stuff
.codex/
.claude/
CLAUDE.local.md
